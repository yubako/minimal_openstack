---
- host: server
  become: yes
  gather_facts: no
  tasks:
    - lineinfile:
        path: /etc/hosts
        line: "127.0.0.1 controller"

- hosts: server
  become: yes
  gather_facts: no
  vars:
    minimal_openstack_controller_management_ip: "127.0.0.1"
  roles:
    - role: ../../
